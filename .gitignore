# ── Tool & session state ─────────────────────────────────────────────
.claude/

# OMC orchestration / session state — tool-generated and recreated by hooks
# (sessions/, state/, project-memory.json, …). Never source; safe to drop.
.omc/

# ── Python artifacts ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ── OS / editor noise ────────────────────────────────────────────────
.DS_Store
*.swp
*~

# ── Build output ─────────────────────────────────────────────────────
# Packaged skill, rebuilt deterministically from loop-optimizer/ via
# skill-creator's package_skill.py (verified byte-identical on rebuild).
# Delete this line if you'd rather commit the .skill as a release artifact.
dist/
dogfood/
handoff/

loop-engineering-analysis.md
loop-engineering-skill-brief.md
loop-engineering-입문가이드.md
loop-optimizer-2차-감사보고서.md
loop-optimizer-audit-report.md

loop-optimizer-workspace/
